<UserControl x:Class="PokerTimer.Win.Controls.InfoCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="200">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="5*" />
            </Grid.RowDefinitions>
            <Viewbox Grid.Row="0" HorizontalAlignment="Left">
                <Label Content="{Binding Text}" Padding="0" Margin="0" FontWeight="Normal" Foreground="White" FontFamily="Arial" />
            </Viewbox>

            <Viewbox Grid.Row="1" HorizontalAlignment="Left" Margin="0,-10,0,20">
                <Label Grid.Row="1" Content="{Binding Message, Mode=TwoWay}" FontSize="48" Padding="0" Margin="0" FontWeight="Normal" Foreground="White" FontFamily="Arial" />
            </Viewbox>
        </Grid>
    </Grid>
</UserControl>